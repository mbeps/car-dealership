/**
 * Default branding asset paths used when Supabase branding metadata is unavailable.
 *
 * These fallbacks keep the header and icon rendering stable before branding data is loaded or persisted.
 */
/**
 * Fallback header logo path.
 */
export const HEADER_LOGO_FALLBACK_SRC = "/logo.png";
/**
 * Fallback favicon path.
 */
export const ICON_FALLBACK_SRC = "/favicon.ico";
/**
 * Fallback light-mode icon path.
 */
export const ICON_SECONDARY_FALLBACK_SRC = "/logo-white.png";
